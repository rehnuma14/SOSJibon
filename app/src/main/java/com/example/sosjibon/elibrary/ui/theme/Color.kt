package com.example.sosjibon.elibrary.ui.theme

import androidx.compose.ui.graphics.Color

// RED — reserved for emergency/critical actions: call button, RED severity tag, CPR/SOS accents
val EmergencyRed = Color(0xFFD32F2F)
val EmergencyRedDark = Color(0xFFB71C1C)
val EmergencyRedLight = Color(0xFFFFEBEE)

// BLUE — informational/trust: app bars, navigation, BLUE severity tag, links
val TrustBlue = Color(0xFF1565C0)
val TrustBlueDark = Color(0xFF0D47A1)
val TrustBlueLight = Color(0xFFE3F2FD)

// GREEN — safe/positive: GREEN severity tag, "do" instructions, success states
val SafeGreen = Color(0xFF2E7D32)
val SafeGreenDark = Color(0xFF1B5E20)
val SafeGreenLight = Color(0xFFE8F5E9)

// Neutrals
val SurfaceLight = Color(0xFFFAFAFA)
val TextPrimary = Color(0xFF1C1C1C)
val TextSecondary = Color(0xFF5F6368)
val CardBorder = Color(0xFFE0E0E0)
